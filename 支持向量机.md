---
created: 2025/11/20 09:51
updated: 2025/11/20 09:57
---
支持向量机
### 1. 线性可分SVM（硬间隔）

- 超平面方程：$$w \cdot x + b = $$（$$$$为权重向量，$$$$为偏置）
    
- 间隔公式：$$\gamma = \frac{2}{|w|$$（目标是最大化$$\gamm$$）
    
- 优化目标：$$\min \frac{1}{2}|w|^$$，约束条件 $$y_i(w \cdot x_i + b) \geq $$（$$y_$$为样本标签$$\pm$$）
    

### 2. 线性不可分SVM（软间隔）

- 引入松弛变量$$\xi_i \geq $$，允许少量样本越界
    
- 优化目标：$$\min \frac{1}{2}|w|^2 + C\sum_{i=1}^n \xi_$$（$$$$为惩罚系数，平衡间隔与误差）
    
- 约束条件：$$y_i(w \cdot x_i + b) \geq 1 - \xi_$$
    

### 3. 核函数与非线性映射

- 核函数定义：$$K(x_i, x_j) = \phi(x_i) \cdot \phi(x_j$$（$$\ph$$为隐式高维映射）
    
- 常见核函数：
    
    - 线性核：$$K(x_i, x_j) = x_i \cdot x_$$
        
    - 多项式核：$$K(x_i, x_j) = (x_i \cdot x_j + 1)^$$（$$$$为多项式次数）
        
    - 高斯核（RBF）：$$K(x_i, x_j) = \exp(-\gamma|x_i - x_j|^2$$（$$\gamma > $$，控制光滑度）